
# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/fluent.conf.erb

@include "#{ENV['FLUENTD_SYSTEMD_CONF'] || 'systemd'}.conf"
@include "#{ENV['FLUENTD_PROMETHEUS_CONF'] || 'prometheus'}.conf"
@include kubernetes.conf
@include conf.d/*.conf

<match **>
  @type couch
  @id out_couch
  @log_level "#{ENV['FLUENT_LOG_LEVEL'] || 'info'}"
  include_tag_key true
  
  host "#{ENV['FLUENT_COUCHDB_HOST']}"
  port "#{ENV['FLUENT_COUCHDB_PORT']}"
  protocol "#{ENV['FLUENT_COUCHDB_PROTOCOL'] || 'http'}"
  database "#{ENV['FLUENT_COUCHDB_DATABASE'] || 'proteinno'}"
  user "#{ENV['FLUENT_COUCHDB_USER']}"
  password "#{ENV['FLUENT_COUCHDB_PASSWORD']}"
</match>